apply plugin: "io.gitlab.arturbosch.detekt"

detekt {
    buildUponDefaultConfig = true
    config = files("$projectDir/../plugins/detekt-config.yml")
    baseline = file("$projectDir/detekt-baseline.xml")

    reports {
        xml.enabled = true
    }
}